import { Box, Typography } from "@mui/material";

export default function Home() {
  return (
    <Box>
      <Typography variant="h4" sx={{ m: '25px' }}>
        This is a simple next.js routing project with a dynamic routing with get params in next.js 14.
      </Typography>
    </Box>
  );
}
